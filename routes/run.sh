#!/bin/sh

#git_user_id=$1
#git_repo_id=$2
#git_branch=$3
#
#if [ "$git_user_id" = "" ]; then
#git_user_id="{{{gitUserId}}}"
#echo "[INFO] No command line input provided. Set \$git_user_id to $git_user_id"
#fi
#
#if [ "$git_repo_id" = "" ]; then
#git_repo_id="{{{gitRepoId}}}"
#echo "[INFO] No command line input provided. Set \$git_repo_id to $git_repo_id"
#fi
#
#if [ "$git_branch" = "" ]; then
#git_branch="master"
#echo "[INFO] No command line input provided. Set \$git_branch $git_branch"
#fi
#
#
#git clone https://github.com/${git_user_id}/${git_repo_id}.git ${git_repo_id}
#
#cd ${git_repo_id}
#
#if [ "$git_branch" != "master" ]; then
#git checkout ${git_branch}
#fi
#

